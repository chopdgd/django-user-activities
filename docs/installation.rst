============
Installation
============

At the command line::

    $ easy_install django-user-activities

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-user-activities
    $ pip install django-user-activities
